# 卡牌与开发玩法优化确认稿

> 状态：已确认并进入实现。此文件记录卡牌布局、视觉资产、山体补充、缺口检测、开发卡预览、道路拖拽、相机与花朵系统的统一规格。

## 1. 屏幕布局

- 手牌固定在屏幕正下方，水平居中，保持在棋盘和右侧信息栏之上。
- 卡牌采用类似斗地主的扇形叠放：后出现、位置更靠右的牌绘制在左侧牌的上方。
- 默认只露出每张牌约 32%-40% 的宽度；鼠标悬停时上移 22 px 并完整显示；选中时上移 34 px。
- 手牌较多时自动减小横向间距，不缩小卡牌本身；最左和最右卡始终能被点到。
- 卡牌区不阻挡右侧信息栏，并根据窗口宽度重新计算居中位置和叠放间距。

## 2. 分类与排序

从左到右固定为四组：

1. 播种卡
2. 开发卡
3. 道路卡
4. 天气卡

组内排序规则：

- 播种卡：等级 1 至 5，从左到右。
- 开发卡：山体开发在前、建筑开发在后；同类按等级升序。
- 道路卡：等级 1 至 3，从左到右。
- 天气卡：台风、沙尘暴、雨季、旱季、彩虹；同名牌保持稳定顺序。
- 新抽到卡牌加入后自动重新排序，但播放短暂的滑入动画，避免位置突然跳变。

## 3. 卡牌尺寸与状态

- 设计基准尺寸：`144 x 204 px`，圆角 `10 px`，接近实体桌游牌比例。
- 默认状态：显示完整底色、暗纹、类别图形、牌名、等级和简短效果。
- 悬停状态：上移、轻微放大至 `1.04`，阴影增强。
- 选中状态：上移并出现玩家颜色描边；再次点击棋盘执行卡牌。
- 不可使用状态：降低饱和度和透明度，但仍可查看文字。
- 已打出状态：向棋盘方向缩小淡出，随后从手牌移除。

## 4. 四类视觉规范

| 类别 | 基础色 | 暗纹色 | 重复图形 | 主图形 |
| --- | --- | --- | --- | --- |
| 播种卡 | `#F7F5EF` 白色 | `#D8D2C5` | 斜向双叶幼苗纹 | 双叶幼苗 |
| 开发卡 | `#A9AFB2` 灰色 | `#7F878B` | 斜向传统锄头纹 | 木柄横刃锄头 |
| 道路卡 | `#E8C34F` 黄色 | `#BE9630` | 斜向维修围栏纹 | 道路围栏 |
| 天气卡 | `#65A9D8` 蓝色 | `#3D82B2` | 斜向云朵纹 | 云朵 |

所有暗纹统一：

- 以约 `-28°` 倾斜排列。
- 图形平行重复，间距保持一致。
- 暗纹透明度约 `16%-22%`，不得干扰文字。
- 主图形位于卡面中上部，采用同色系深色，不使用高亮发光。

## 5. 卡面信息层级

- 左上角：等级或天气类型符号。
- 上部：卡牌名称。
- 中部：类别主图形。
- 下部：一行精简效果文字。
- 底边：类别名称，用于颜色辨识之外的辅助识别。

## 6. 资源文件

- `assets/card-previews/card-style-preview.svg`：四类卡牌并排总览。
- `assets/card-previews/card-seed.svg`：播种卡基础模板。
- `assets/card-previews/card-development.svg`：开发卡基础模板。
- `assets/card-previews/card-road.svg`：道路卡基础模板。
- `assets/card-previews/card-weather.svg`：天气卡基础模板。

这些文件是视觉确认稿，不会在确认前接入游戏。

## 7. 山体补充与缺口规则

### 7.1 取消正方形补边

删除当前整圈填满山体的方案。棋盘存储允许动态扩张，并用明确的空值区分“没有地块”，使可见棋盘保持不规则轮廓。

每次成功使用山体开发卡后：

1. 取得当前所有非可开发地块的坐标快照。非可开发地块包括森林、草地、荒漠、水域和建筑。
2. 对快照中的每个坐标检查以它为中心的 `5×5` 范围，即横纵方向各延伸两格。
3. 范围内已经存在的植物、增益、山体或缺口全部保持原样。
4. 仅将真正“不存在地块”的坐标补为山体。
5. 新补出的山体不在同一次处理中继续向外扩散，避免一次操作无限生成。

结果会形成沿着已开发区域轮廓向外延伸两格的山体缓冲带，不再是固定正方形边框。凸出、转弯和不规则开发区会得到对应的不规则山体外围。

### 7.2 缺口检测

缺口不是单格规则，而是开发后的非可开发地块之间形成的封闭空腔。空腔可能只有一格，也可能是任意形状、任意数量的连续多格区域。

缺口候选区域包括：

- 被开发区域包围的现有山体。
- 被开发区域包围、当前不存在地块的空坐标。
- 多个通过上下左右相连的候选格视为同一个完整区域。

系统在开发区域的外接范围之外增加安全边界，然后执行区域洪水检测：

1. 从安全边界开始，在山体和不存在地块的坐标之间进行上下左右搜索。
2. 能够从安全边界连通到的区域属于外部空间，继续保持山体或空坐标。
3. 无法连通到安全边界的候选区域属于内部空腔。
4. 再检查该空腔的全部边界；边界必须完全由森林、草地、荒漠、水域或建筑组成。
5. 满足条件后，将空腔内的所有候选格整体转换为透明缺口地块。

因此，环形开发区内部的 `2×3` 空腔会一次生成六格连续缺口，L 形或弯曲空腔也会保持原来的形状，不要求每个缺口格自己单独满足“四面包围”。斜角接触不算连通，区域连接与包围判断统一使用上下左右四方向。

处理顺序固定为：

1. 开发卡生成新地块。
2. 在当前开发轮廓内执行封闭区域洪水检测。
3. 将所有符合条件的单格或多格空腔整体转换为透明缺口，并立即刷新模型。
4. 再按非可开发地块快照补充 `5×5` 范围内缺失的山体。
5. 补山完成后再次检测一次，处理本次开发与原有开发区共同形成的新封闭空腔。

补充山体不会覆盖已经生成的缺口；缺口是明确存在的透明地块，并非“空坐标”。缺口区域形成后保持原形，除非玩家使用建筑开发卡将其中的格子转换为建筑。

### 7.3 数据结构调整

现有平行二维数组增加动态扩容和空坐标语义，在行为上形成稀疏棋盘：

- 地形类型、道路连接、花朵数量和场景节点仍按坐标存取，空坐标不会生成模型。
- 鼠标拾取直接返回世界坐标对应的棋盘坐标，不再受数组边界限制。
- 相机可继续平移和缩放，棋盘只为已存在的坐标生成模型。
- 计分、天气、花朵增长和道路遍历只遍历现存地块。

## 8. 开发卡形状与放置预览

### 8.1 卡面显示形状

- 每张山体开发卡在卡面中央明确显示其开发区块形状。
- 1级显示单格，2级显示两格直线，3级显示三格 L 形。
- 4级卡在生成卡牌时就确定为 `2×2`、Z 形或 T 形中的一种，并在卡面显示实际形状。
- 建筑开发卡同样显示 `1×1` 或 `1×2` 形状，并使用建筑标记与山体开发卡区分；1级建造黄鹤楼，2级在两个相邻缺口上建造一座连续的洪山科技大厦。

### 8.2 拖拽和随机地形

1. 玩家从屏幕底部按住开发卡并拖向棋盘。
2. 卡牌第一次离开手牌区时，系统为形状内每一格随机生成具体地形，并保存为该卡本次的待放置结果。
3. 棋盘上显示完整的半透明 2.5D 地块预览，不只显示纯色方块；水域、森林、草地、荒漠和建筑都能直接辨认。
4. 移动鼠标时，整组预览吸附到棋盘格。
5. 使用 `Q/E` 旋转整组形状，旋转只改变位置和道路方向，不重新随机地形。
6. 可放置时使用柔和绿色轮廓；不可放置时使用红色轮廓，并明确显示冲突格。
7. 松开鼠标后才正式放置并消耗卡牌；在无效位置松开则返回手牌。

为了避免反复取消来刷新随机结果，某张卡第一次生成的待放置地形会一直保留到它成功使用或回合结束，不会因旋转、移动或拖回手牌而重抽。

### 8.3 放置限制

- 形状内所有格必须落在现有山体上。
- 不可覆盖植物、增益、建筑、缺口或不存在的坐标。
- 整组形状至少有一格与非可开发地块上下左右相邻。
- 预览使用与正式放置完全相同的校验函数，避免预览可放但松手失败。

## 9. 左上角公共卡堆与回合抽牌

### 9.1 三个公共卡堆

页面左上角固定显示三个可点击卡堆，从左到右为：

1. 开发卡堆
2. 道路卡堆
3. 天气卡堆

播种卡不设公共卡堆，仍然只在开局发放。三个卡堆沿用对应卡牌的灰色、黄色、蓝色视觉，并显示各自的暗纹和主图形。卡背不展示即将抽到的具体等级或类别。

### 9.2 每回合抽取三次

- 每位玩家回合开始时先进入“抽牌阶段”，必须完成三次抽牌。
- 玩家每次点击任意一个卡堆抽取一张牌，可以自由组合来源。
- 允许连续三次点击同一卡堆，也允许从三个卡堆各抽一张。
- 左上角显示清晰的抽牌进度，例如 `本回合抽牌 1 / 3`。
- 抽满三张后，三个卡堆进入不可点击状态，游戏自动切换到出牌阶段。
- 未抽满三张时不能使用手牌或结束回合。
- 新卡从对应卡堆飞向屏幕底部，并在到达手牌后按照类别、等级和子类型重新排序。
- 如果玩家快速连续点击，抽牌动画按队列播放，但计数会立即锁定，避免多抽。

### 9.3 卡堆交互状态

- 默认：显示完整卡背、类别名称和轻微堆叠厚度。
- 悬停：卡堆上移并加深阴影，鼠标指针反馈可点击。
- 点击：顶部卡牌短暂压下，然后飞入手牌。
- 已抽满：整体降低亮度，并显示 `3 / 3` 完成状态。
- 当前不是抽牌阶段：仍显示卡堆，但不响应点击，避免界面突然移动。

### 9.4 回合状态流程

`回合开始 → 抽牌阶段（点击三次）→ 出牌阶段（可出任意张）→ 结算 → 下一位玩家`

每次进入新玩家回合时抽牌计数重置为 `0 / 3`。现有自动随机发三张牌的逻辑将被移除。

## 10. 自由画面移动

### 10.1 移动方式

- 中键拖拽改为基于相机屏幕平面的自由移动，不再把屏幕横纵位移直接绑定到棋盘世界 `X/Z` 轴。
- 水平拖动沿相机画面的右方向移动；垂直拖动沿相机画面的前后方向在地面上的投影移动。
- 斜向或曲线拖动会得到连续的任意方向位移，不再出现只能沿轴移动的感觉。
- 触控板双指平移使用完全相同的坐标换算。
- 移动速度根据当前缩放自动调整：拉远时移动更快，拉近时更精细。

### 10.2 操作反馈

- 按下中键后立即锁定拖拽起点，避免画面第一帧跳动。
- 使用平滑阻尼跟随鼠标，但缩短延迟，保证移动不发飘。
- 拖拽期间暂停地块悬停检测，松开后立即恢复。
- `C` 键仍可让相机平滑返回棋盘初始中心和默认缩放。
- 不对自由移动设置硬性矩形边界，以适配不规则扩张的无限棋盘。

### 10.3 坐标换算

实现时从相机变换取得屏幕右向量，并将相机前向量投影到水平地面；鼠标二维位移分别乘以这两个向量后相加，得到最终三维平移量。相机位置使用三维地面偏移保存，不再只保存简单的二维棋盘轴偏移。

## 11. 四名玩家的花朵与增量结算

### 11.1 四种玩家花型

四名玩家不仅使用不同颜色，也使用不同轮廓，缩小到棋盘视角后仍可辨认：

| 玩家 | 对应颜色 | 花型 | 主要轮廓 |
| --- | --- | --- | --- |
| 玩家1 | 红色 | 虞美人 | 四片宽花瓣、深色花心、细长花茎 |
| 玩家2 | 紫色 | 风铃草 | 下垂钟形花冠、弯曲花茎 |
| 玩家3 | 橙色 | 万寿菊 | 多层紧密圆瓣、较饱满的花头 |
| 玩家4 | 深蓝色 | 鸢尾花 | 三片向上与三片舒展花瓣、剑形叶片 |

- 花瓣主色严格使用玩家颜色，并允许花心、花茎和叶片使用同色系明暗变化。
- 四种花保持相近的视觉体积，避免某个玩家因为模型更大而显得数量更多。
- 模型保持低面数和小尺寸，以便单块地显示大量花朵。

### 11.2 随机而自然的分布

- 取消目前规则的 `10×10` 网格排列。
- 每朵新花生成时，从地块可种植区域中取得一个随机位置。
- 使用带最小间距的随机采样，避免大量花完全重叠，也避免重新形成明显网格。
- 道路范围、建筑底座、树干和地块边缘保留避让区，花朵不会穿进道路或主要模型。
- 同一批花可形成少量自然的小簇，但整体密度仍反映真实数量。
- 每朵花的整体大小在基准值约 `75%-125%` 之间变化。
- 花茎高度、花冠方向和绕 Y 轴旋转轻微随机，使花群看起来不重复。
- 随机结果在花朵存续期间永久保存；相机移动、地形刷新和普通结算不会改变位置。

### 11.3 增量增加花朵

每个植物地块为四名玩家分别保存花朵实例列表。结算或播种后：

1. 比较结算前数量与结算后数量。
2. 若某玩家增加 `N` 朵，只创建 `N` 个新实例。
3. 新花从接近地面的缩小状态生长到随机目标大小，并带有轻微弹性和摆动。
4. 原有花朵、位置、大小和朝向完全不变。
5. 数量数据与场景实例数量完成同步后再更新计分显示。

### 11.4 增量删除花朵

当降级、天气或容量变化导致花朵减少时：

1. 计算每名玩家实际需要删除的数量。
2. 从该玩家现存实例中随机选取对应数量，避免每次只从固定边缘消失。
3. 被删除的花先缩小、下沉并降低透明度，再释放实例。
4. 未被删除的花保持原位，不重新生成。
5. 若地块变为不能容纳花朵的类型，则按短暂分批动画移除全部花朵。

### 11.5 数据与性能

- 花朵逻辑数量仍以现有规则数据为准，场景实例只是其视觉映射。
- 每朵花保存稳定编号、玩家、位置、大小、旋转和模型类型。
- 使用按“地块 + 玩家 + 花型”分组的实例管理，减少大量独立节点造成的性能压力。
- 地块重绘只更新地形模型，不删除花朵容器；只有花朵数量变化才调用增删逻辑。
- 增量同步函数会检查数据与实例差值，防止动画中重复添加或删除。

## 12. 连续道路生成与拖拽修路

### 12.1 开发卡随机道路

开发卡随机生成地块后，可以在新生成的非可开发地块上附带道路，但道路必须按完整连接关系生成：

- 先确定开发形状内实际生成的森林、草地、荒漠、水域和建筑地块。
- 只在上下左右相邻的非可开发地块之间生成道路连接。
- 每一段跨越地块边缘的道路必须同时写入两侧地块：一侧向右，则右侧地块必须同时向左连接。
- 禁止为单个地块独立生成随机方向端口，禁止道路伸向山体、缺口或不存在地块。
- 随机道路先选择一个起点，再沿相邻地块生成连续路径；后续可形成转弯、分支或与已有道路接通，但不能生成彼此分离的碎片。
- 如果本次开发形状里不足两个相邻的非可开发地块，就不生成随机道路。
- 单格开发卡只有在能够同时接入相邻已有道路、形成有效连续连接时才允许附带道路，否则保持无道路。
- 随机道路与随机地形一样，在开发卡拖出手牌时确定，并显示在放置预览中；旋转时道路方向随形状一起旋转。

放置完成后执行完整性校验：遍历所有道路端口，确保相邻格存在反向端口。发现无法配对的端口时不保留该端口，从数据层避免断头道路。

这里的“不断头”具体指道路不能在地块边缘突然消失：凡是道路到达地块边缘，对面地块必须有对应连接。整条道路本身仍可拥有起点和终点，但起终点会在地块内部使用完整的收口模型，不会画成伸到边缘后悬空的半截道路。

### 12.2 道路卡拖拽建造

道路卡改为类似《城市：天际线》的连续拖动修路方式：

1. 玩家从底部手牌按住道路卡并拖到一个合法地块，确定道路起点。
2. 保持按住鼠标，在棋盘上依次经过上下左右相邻地块。
3. 系统实时记录经过顺序并绘制半透明道路预览。
4. 路径可以直行，也可以连续转弯，不再受单一方向限制。
5. 松开鼠标后才正式生成道路并消耗卡牌。
6. 在非法位置松开或长度不满足卡牌要求时，取消预览并将卡牌返回手牌。

### 12.3 道路卡等级与路径长度

- 1级道路卡：拖出 1 段，连接 2 个地块。
- 2级道路卡：拖出 2 段，连续连接 3 个地块。
- 3级道路卡：拖出 3 段，连续连接 4 个地块。
- 必须达到卡牌规定的准确段数后才能松开建造。
- 拖回路径中的上一个地块时撤销最后一段，便于玩家修正路线。
- 不允许在同一条新路径中重复经过同一地块，也不允许通过斜角跨格。
- 鼠标移动过快跨过多个格子时，只接受能够还原成连续正交路径的部分，不能跳格连线。

### 12.4 可作用地块

道路卡可以经过所有植物地块和增益地块：

- 森林
- 草地
- 荒漠
- 水域
- 建筑

道路卡不能经过山体、缺口或不存在的地块。已有道路的地块可以再次经过，从而延长道路、形成转弯、T 形路口或十字路口。

### 12.5 道路预览与模型

- 当前有效路径显示黄色半透明道路，路径终点显示明显的建造节点。
- 非法延伸位置显示红色，不会写入路径。
- 道路模型根据每格的连接掩码自动选择直线、转角、T 形和十字形外观。
- 所有相邻道路在地块边缘使用相同宽度和高度，避免视觉断裂、错位或穿模。
- 正式放置时道路按拖动顺序逐段出现，形成连续施工动画。
- 道路闭合后再触发现有的闭合效果，预览阶段不提前结算。

## 13. 确认后实施范围

1. 将底部手牌区改为可响应窗口尺寸的扇形叠牌布局。
2. 增加稳定的四类/等级排序函数。
3. 增加悬停、选中、不可用、抽牌和出牌动画。
4. 将确认后的 SVG 模板转为正式卡牌资源并接入 Godot。
5. 更新鼠标命中区域，保证重叠卡牌优先选中最上层卡。
6. 保留键盘操作，并让键盘选择与卡牌动画同步。
7. 将固定二维棋盘改为可动态扩容、支持空坐标的不规则棋盘。
8. 删除正方形补边，加入围绕非可开发地块的 `5×5` 局部山体补充。
9. 将缺口改为封闭空腔洪水检测，支持单格和连续多格缺口，并修复透明模型刷新与补山覆盖问题。
10. 为开发卡加入形状图案、拖拽、随机地形预览、旋转和放置动画。
11. 在左上角加入开发、道路、天气三个公共卡堆。
12. 将回合开始改为玩家自由点击卡堆抽取三次，并加入进度锁定和抽牌动画。
13. 将相机拖拽改为基于屏幕方向的自由平移，并统一鼠标与触控板操作。
14. 为四名玩家制作虞美人、风铃草、万寿菊和鸢尾花四种低模花型。
15. 将花朵分布改为带避让和最小间距的持久随机布局，并加入尺寸、朝向和高度变化。
16. 将花朵显示改为按结算差值增量创建和删除，不再整块刷新。
17. 重写开发卡随机道路生成，使所有跨格端口成对写入并保持为连续网络。
18. 将道路卡改为类似城市建设游戏的按住拖动、实时预览、可转弯连续修路。
19. 重建道路直线、转角、T 形和十字形模型连接，消除断头、错位和穿模。

## 14. 待确认项

- 卡牌比例、四种基础色和暗纹密度是否合适。
- 手牌是否采用“水平叠放、右牌压左牌”的方案。
- 卡牌文字是否保留“名称 + 一行效果 + 类别”，还是进一步精简。
- 开发卡是否需要把山体开发与建筑开发做成两套不同主图形。
- 开发卡采用“拖出手牌时确定随机地形，取消拖拽不重新随机”的规则。
- 山体补充范围采用每个非可开发地块周围 `5×5`，且新山体当次不继续扩散。
- 缺口采用区域包围规则：由非可开发地块完全包围的整片内部空腔全部转为缺口，不限制为单格。
- 左上角三个卡堆始终可见，每回合允许任意组合抽取三张，抽满后自动进入出牌阶段。
- 画面移动采用相机屏幕空间自由平移，不设置固定轴向或硬边界。
- 四名玩家花型采用红色虞美人、紫色风铃草、橙色万寿菊、深蓝色鸢尾花。
- 花朵位置在生成时随机并永久保存；结算只增删发生变化的花朵实例。
- 开发卡自带道路只在相邻非可开发地块之间成对生成，不允许任何未配对的地块边缘端口。
- 道路卡采用按住鼠标连续经过相邻地块的拖拽建造方式，允许直行、转弯和接入已有道路。
