# 手牌与模型预览优化计划

## 1. 卡牌层次

- 顺序固定为：投影、白色衬底、无边框渐变牌面、模型化主体、文字、前后层级遮罩。
- 白色衬底向右下偏移，必须位于渐变牌面下方、黑色投影上方。

## 2. 播种卡

- 主体使用地块花朵相同的“茎、花心、放射花瓣”造型语言和当前玩家颜色。
- 等级决定花束数量：1级1朵、2级2朵、3级3朵，其余等级最多显示5朵。
- 花朵大小、明暗略有变化，但保持可读性。

## 3. 开发卡

- 使用卡牌保存的 `level` 与 `shape` 读取真实开发形状。
- 每个形状格绘制为等距山体地块：地块顶板、侧面和多峰山体，拼接关系与放置形状一致。

## 4. 建筑开发卡

- 1级显示黄鹤楼：红色五层楼身、金色多重飞檐和塔刹。
- 2级显示洪山科技大厦：两个相连体块、白色框架、蓝色玻璃幕墙和连廊。

## 5. 天气卡

- 根据卡牌 `weather` 分别绘制台风、沙尘暴、雨季、旱季和彩虹预告图标。
- 图标沿用场景天气配色和形状：旋风雨线、沙粒风带、云雨、水滴裂地、上半彩虹。

## 6. 右下角3D预览

- 使用透明 `SubViewport` 和正交相机显示真实3D模型，不再绘制二维地形色块。
- 直接复用 `_spawn_island_base()`、各 `_tile_*_surface()`、`_spawn_decor()`、建筑和道路函数。
- 开发卡显示已经随机确定的地形和道路；建筑开发卡显示对应黄鹤楼或完整双格科技楼。
- Q/E旋转后重建预览，模型形状、地形、道路方向和最终放置保持一致。

## 验收

- 任意手牌都能明确从主体图案辨认类型、等级或天气。
- 卡面没有外边框，底部能看到白色衬底和更下层阴影。
- 开发形状与建筑等级不会使用通用占位符。
- 右下角展示可旋转的真实模型，和棋盘落地模型使用同一组构建函数。
