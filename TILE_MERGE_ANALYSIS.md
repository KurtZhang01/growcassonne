# 同类地块边界延展与缝隙分析

---

## 一、地块结构完整尺寸表

### TILE_SPACING = 1.25（地块中心间距）

```
地块中心 ──── 0.625 ──── 缝隙中点 ──── 0.625 ──── 邻居中心
```

### 底座各层尺寸

| 层 | 宽度 | 中心Y | 高度 | Y范围 | ±范围 |
|----|------|-------|------|-------|-------|
| Cap（封顶层） | 1.03 | 0.12 | 0.06 | 0.09~0.15 | ±0.515 |
| Top（顶层平台） | 1.06 | 0.025 | 0.13 | -0.04~0.09 | ±0.53 |
| Mid（中层） | 0.88 | -0.095 | 0.12 | -0.155~-0.035 | ±0.44 |
| Bot（底层） | 0.56 | -0.225 | 0.16 | -0.305~-0.145 | ±0.28 |
| Core（核心柱） | 顶端r=0.32 | -0.39 | 0.32 | -0.55~-0.23 | 六棱锥 |

### 地形表面尺寸

| 地形 | 表面宽度 | 表面Y | 表面高度 | ±范围 |
|------|----------|-------|----------|-------|
| 草地 | 0.95 | 0.13 | 0.06 | ±0.475 |
| 森林 | 0.95 | 0.138 | 0.075 | ±0.475 |
| 荒漠 | 0.95 | 0.13 | 0.05 | ±0.475 |
| 水面 | 0.92 | 0.10 | 0.04 | ±0.46 |

### 边框尺寸

| 项目 | 宽度 | 中心位置 | ±范围 |
|------|------|----------|-------|
| 边框（水平方向） | 1.09 | ±0.53 | ±0.545 |
| 边框（垂直方向） | 0.03 | ±0.53 | ±0.515~±0.545 |

---

## 二、缝隙计算

### 相邻地块中心间距 = 1.25

```
地块A中心 ──0.53── A的top层边缘 ──0.19── 缝隙中点 ──0.19── B的top层边缘 ──0.53── 地块B中心
           |←── 0.625 ──────────────────→|←── 0.625 ──────────────────→|
```

### 各层之间的缝隙

| 比较项 | A的边缘 | B的边缘 | 缝隙宽度 |
|--------|---------|---------|----------|
| Top层（1.06宽） | +0.53 | 1.25-0.53=0.72 | **0.19** |
| Cap（1.03宽） | +0.515 | 1.25-0.515=0.735 | **0.22** |
| 草地表面（0.95宽） | +0.475 | 1.25-0.475=0.775 | **0.30** |
| 水面（0.92宽） | +0.46 | 1.25-0.46=0.79 | **0.33** |
| 边框（1.09宽） | +0.545 | 1.25-0.545=0.705 | **0.16** |

**结论**：
- Top层之间缝隙 **0.19**，被Cap（1.03宽）覆盖，Cap之间缝隙 **0.22**
- 地形表面之间缝隙 **0.30~0.33**，非常大，视觉上两个同类地块之间有明显空隙
- 边框之间缝隙 **0.16**，边框没有延伸到缝隙中

---

## 三、同类地块连接的三个问题

### 问题1：同类地块表面不连接

当两个草地相邻时：
- 草地表面宽0.95（±0.475）
- 两个草地表面之间的缝隙 = 1.25 - 0.475 - 0.475 = **0.30**

视觉上两个草地之间有 **0.30** 宽的空隙，能看到下面的底座侧面，破坏了"同类地形连成一片"的感觉。

**已尝试的方案**：将表面扩展到1.06（与top层齐平），缝隙减小到0.19。但被用户回退（可能其他视觉问题）。

**根因**：地形表面（0.95）比底座top层（1.06）窄太多。需要一个**过渡层**来连接。

### 问题2：同类地块底座之间有缝隙

两个同类地块的top层（1.06宽）之间缝隙0.19，被Cap（1.03宽）覆盖。Cap之间缝隙0.22。

Cap的作用是覆盖top层之间的接缝，但Cap本身也有缝隙。

**已尝试的方案**：边框加宽到0.72、中心移到±0.625，被用户回退。

### 问题3：边框悬浮

当前边框（1.09×0.14）中心在±0.53，y=0.10。边框底部在y=0.03，top层底部在y=-0.04。边框比top层窄（只覆盖0.14高度），且位置偏高，看起来像"漂浮"在地块上方。

---

## 四、可能的解决方案

### 方案A：地形表面扩展 + 渐变过渡

将地形表面从0.95扩展到1.06（与top层齐平），但不是突然变宽，而是在边缘添加**渐变过渡条**：

```
主表面（0.95宽，不透明）+ 过渡条（0.055宽，半透明渐变，每侧）
总宽度 = 0.95 + 0.055*2 = 1.06
```

过渡条使用同色但alpha从1.0渐变到0.0，视觉上自然融入。

**优点**：同类地块表面在±0.53处完全连接，无缝隙
**缺点**：水面需要特殊处理（已有方案：水面连接条）

### 方案B：底座侧面覆盖（边框改为全高侧面）

将边框从"薄片"改为"全高侧面板"，覆盖底座top层到bot层的整个侧面：

```
当前边框：1.09×0.14×0.03（薄片，只覆盖top层上部）
改为：1.09×0.30×0.06（厚板，覆盖top层到bot层）
中心Y：从0.10改为-0.04（覆盖整个侧面）
```

**优点**：边框完全覆盖底座侧面，不再悬浮
**缺点**：边框太厚可能影响视觉；且同类地块之间仍需要判断是否显示边框

### 方案C：Cap加宽 + 边框嵌入底座

Cap从1.03加宽到1.09（与边框同宽），边框嵌入Cap内：

```
Cap：1.09宽（±0.545），覆盖整个缝隙区域
边框：1.09×0.14，中心在±0.53，嵌入Cap内
```

**优点**：Cap覆盖了缝隙，边框不再悬浮
**缺点**：Cap加宽后，两个Cap之间缝隙=1.25-1.09=0.16，仍存在小缝隙

### 方案D：边框位置对齐Cap（最简单）

将边框从y=0.10改为y=0.12（与Cap对齐），高度从0.14改为0.18（覆盖Cap到top层）：

```
边框：1.09×0.18，中心在±0.53，y=0.12
边框底部：y=0.03（覆盖top层顶部）
边框顶部：y=0.21（超过Cap顶部0.15）
```

**优点**：边框与Cap融合，不再悬浮
**缺点**：同类地块之间表面缝隙（0.30）问题仍未解决

### 方案E：组合方案（推荐）

1. **地形表面扩展**：草地/森林/荒漠表面从0.95扩展到1.06（与top层齐平）
2. **边框对齐Cap**：边框y从0.10改为0.12，高度从0.14改为0.18
3. **水面连接条**：保持现有方案（0.28宽，±0.53中心）

这样：
- 同类地块表面在±0.53处完全连接，无缝隙
- 边框与Cap融合，覆盖底座侧面
- 水面有专用连接条

---

## 五、L形转角分析（补充）

### 转角交汇点（四格交叉处）

```
┌─────────┬─────────┐
│  草地 A  │  草地 B  │
├─────────●─────────┤
│  草地 C  │  沙漠 D  │
└─────────┴─────────┘
```

交汇点●在A/B/C/D四格中心的缝隙中点。

**当前边框行为**：
- D的左边框（分隔D与C）：覆盖D的左侧边
- D的上边框（分隔D与B）：覆盖D的上侧边
- 两条边框在●处相交，形成L形接头

**同类地块行为**：
- A与B同类 → 无边框
- A与C同类 → 无边框
- B与D不同类 → B的下边框 + D的上边框（在●附近重叠）

**结论**：边框在转角处自然相交，无需额外角部处理。但同类地块之间的表面缝隙问题（问题1）仍然存在。

---

## 六、最终建议

**优先实施方案E**（组合方案），分两步：

**第一步**（最小改动）：边框对齐Cap
- 边框y从0.10改为0.12
- 边框高度从0.14改为0.18
- 不改变地形表面宽度

**第二步**（完整方案）：地形表面扩展
- 草地/森林/荒漠表面从0.95扩展到1.06
- 水面保持现有连接条方案
- 验证无视觉问题后提交
