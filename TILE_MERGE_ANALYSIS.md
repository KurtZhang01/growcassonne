# 同类地块边界延展与缝隙分析

---

## 一、地块结构完整尺寸表

### 基础常量

- `TILE_SPACING` = 1.25（地块中心间距）
- 半间距 = 0.625（中心到缝隙中点）

### 底座各层尺寸（_spawn_island_base）

| 层 | 宽度 | 中心Y | 高度 | Y范围 | ±范围 |
|----|------|-------|------|-------|-------|
| Cap（封顶层） | 1.03 | 0.12 | 0.06 | 0.09~0.15 | ±0.515 |
| Top（顶层平台） | 1.06 | 0.025 | 0.13 | -0.04~0.09 | ±0.53 |
| Mid（中层） | 0.88 | -0.095 | 0.12 | -0.155~-0.035 | ±0.44 |
| Bot（底层） | 0.56 | -0.225 | 0.16 | -0.305~-0.145 | ±0.28 |
| Core（核心柱） | 顶端r=0.32 | -0.39 | 0.32 | -0.55~-0.23 | 六棱锥 |

**注意**：水域地块不生成Cap（`if terr != T_WATER`）

### 地形表面尺寸

| 地形 | 表面宽度 | 表面Y | 表面高度 | ±范围 |
|------|----------|-------|----------|-------|
| 草地 | 0.95 | 0.13 | 0.06 | ±0.475 |
| 森林 | 0.95 | 0.138 | 0.075 | ±0.475 |
| 荒漠 | 0.95 | 0.13 | 0.05 | ±0.475 |
| 水面 | 0.92 | 0.10 | 0.04 | ±0.46 |

### 边框尺寸

| 项目 | 宽度(水平) | 宽度(垂直) | 中心位置 | ±范围 |
|------|-----------|-----------|----------|-------|
| 边框 | 1.09 | 0.03 | ±0.53 | ±0.545(水平) / ±0.515~±0.545(垂直) |

---

## 二、缝隙计算

### 相邻地块中心间距 = 1.25

```
地块A中心 ──0.53── A的top层边缘 ──0.19── 缝隙中点 ──0.19── B的top层边缘 ──0.53── 地块B中心
           |←── 0.625 ──────────────────→|←── 0.625 ──────────────────→|
```

### 各层之间的缝隙

| 比较项 | A的边缘 | B的边缘 | 缝隙宽度 |
|--------|---------|---------|----------|
| Top层（1.06宽） | +0.53 | 1.25-0.53=0.72 | **0.19** |
| Cap（1.03宽） | +0.515 | 1.25-0.515=0.735 | **0.22** |
| 草地表面（0.95宽） | +0.475 | 1.25-0.475=0.775 | **0.30** |
| 水面（0.92宽） | +0.46 | 1.25-0.46=0.79 | **0.33** |
| 边框（1.09宽） | +0.545 | 1.25-0.545=0.705 | **0.16** |

**关键发现**：
- 同类地块表面之间缝隙 **0.30~0.33**，非常大
- Top层之间缝隙 **0.19**，被Cap覆盖（Cap之间仍有0.22缝隙）
- 边框之间缝隙 **0.16**，边框没有延伸到缝隙中

---

## 三、同类地块连接的三个核心问题

### 问题1：同类地块表面不连接（最严重）

两个草地相邻时：
- 草地表面宽0.95（±0.475）
- 两表面之间缝隙 = 1.25 - 0.475 - 0.475 = **0.30**

0.30的缝隙意味着同类草地之间有明显空隙，能看到底座侧面。

**已尝试**：扩展到1.06（与top层齐平）→ 被用户回退

### 问题2：底座Cap之间仍有缝隙

Cap（1.03宽）之间缝隙 = 1.25 - 1.03 = **0.22**。Cap覆盖了top层之间的缝隙，但Cap本身之间也有缝隙。

### 问题3：边框悬浮

边框（1.09×0.14）中心在±0.53，y=0.10。边框底部y=0.03，top层底部y=-0.04。边框比top层短（只覆盖0.14高度），看起来"漂浮"在地块上方。

---

## 四、可能的解决方案

### 方案A：地形表面扩展 + 渐变过渡（被回退过）

地形表面从0.95扩展到1.06（与top层齐平），边缘加渐变过渡条。

**状态**：用户已回退此方案两次。

### 方案B：Cap加宽覆盖缝隙

Cap从1.03加宽到1.09（与边框同宽），缝隙从0.22减到0.16。

**问题**：水域没有Cap，水面缝隙仍然0.33。

### 方案C：边框对齐Cap（微调）

边框y从0.10改为0.12（与Cap对齐），高度从0.14改为0.18（覆盖Cap到top层）。

**效果**：边框与Cap融合，不再悬浮。但同类表面缝隙0.30问题未解决。

### 方案D：水面连接条（已有方案）

水域之间添加连接条（0.28宽，±0.53中心），使用半透明蓝色材质。

**状态**：已在代码中实现，待验证效果。

### 方案E：组合方案（推荐）

1. **边框对齐Cap**：y 0.10→0.12，高度 0.14→0.18
2. **保持地形表面原始宽度**（0.95），不扩展
3. **水面连接条**保持现有方案
4. **Cap微调**：1.03→1.06（与top层齐平），减少Cap间缝隙到0.19

**效果**：
- 边框与Cap融合 ✓
- Cap缝隙从0.22减到0.19 ✓
- 水面有连接条 ✓
- 同类表面缝隙0.30问题仍存在（但视觉上被Cap/边框遮挡大部分）

---

## 五、L形转角分析

### 转角交汇点（四格交叉处）

```
┌─────────┬─────────┐
│  草地 A  │  沙漠 B  │
├─────────●─────────┤
│  沙漠 C  │  沙漠 D  │
└─────────┴─────────┘
```

交汇点●在四格中心的缝隙中点。

**边框行为**：
- D的左边框（分隔D与C）：同类，不生成 ✓
- D的上边框（分隔D与B）：同类，不生成 ✓
- A的右边框（分隔A与B）：不同类，生成
- A的下边框（分隔A与C）：不同类，生成
- 两条边框在●处相交，形成L形接头

**结论**：边框在转角处自然相交，无需额外角部处理。

---

## 六、最终建议

**实施方案E**，分两步：

**第一步**（最小改动，解决边框悬浮）：
- 边框y从0.10改为0.12
- 边框高度从0.14改为0.18
- 不改变地形表面宽度

**第二步**（减少Cap缝隙）：
- Cap从1.03加宽到1.06（与top层齐平）
- 缝隙从0.22减到0.19

**第三步**（如用户接受）：
- 地形表面从0.95扩展到1.06（彻底解决同类连接）
