# 地块顶层、边框与道路接缝修订方案

## 实施记录

本方案已接入 `scripts/main3d.gd`：中层和底层恢复独立尺寸，顶层使用唯一连接片；地块类型改变后重建自身及四邻顶层接缝；道路接缝改为双层同色模型；有道路的森林使用避路小苔藓斑块。实际画面仍需在允许运行 Godot 的环境中验收。

## 1. 先说明当前模型的几层分别是什么

当前每个地块从上到下由以下部分组成：

| 名称 | 当前作用 | 大致位置 | 是否需要与相邻地块连接 |
|---|---|---:|---|
| 装饰层 | 树木、草丛、石块、建筑、花朵和道路等立体内容 | `y > 0.16` | 不直接连接；只需要避开道路和边界 |
| 顶层 | 玩家看到的地形表面，决定草地、水域、森林等颜色 | `y = 0.09~0.16` | 需要连接 |
| 边框 | 顶层外围的细侧边，用来区分不同地形并遮住表面边缘 | `y = 0~0.18` | 需要在接缝处闭合；同类地形之间隐藏 |
| 中层 | 浮岛侧面的第二级体块，制造向下收缩的悬浮感 | `y ≈ -0.095` | 不连接，恢复单块轮廓 |
| 底层 | 更靠下、更窄的体块，让地块像浮岛而不是厚棋盘 | `y ≈ -0.225` | 不连接，恢复单块轮廓 |
| 岩芯 | 最下方的多边形锥体，是单个浮岛的视觉重心 | `y ≈ -0.39` | 不连接 |

“装饰层”不是另一块连续底板，它是放在顶层上的模型集合。因此本轮不再提出装饰层底板连接，也不连接中层和底层。

## 2. 用户确认后的地块连接范围

只处理两部分：

1. 顶层地形表面。
2. 顶层外围边框。

中层、底层和岩芯恢复为每个地块独立的收缩形状。这样从上方看，地形区域能够连成一片；从侧面看，仍保留多个悬浮地块的层次，不会变成一整块方形厚板。

## 3. 顶层连接方案

### 3.1 不直接扩大所有底座

`TILE_SPACING` 是 `1.25`。当前提交把上、中、下三层全部扩大到 `1.25 x 1.25`，这会消除空隙，但也抹掉浮岛层次。需要撤回中层和底层的扩大。

建议恢复：

| 部分 | 建议尺寸 |
|---|---|
| 顶层主体 | 原地块表面尺寸，约 `1.03 x 1.03` |
| 中层 | `0.88 x 0.88` |
| 底层 | `0.56 x 0.56` |
| 岩芯 | 保持现状 |

### 3.2 使用独立顶层连接片

在两个相邻有效地块之间，仅生成一块位于顶面的连接片：

- 接缝方向长度：`TILE_SPACING - TOP_SIZE + 0.04`，约 `0.26`。
- 接缝横向宽度：约 `1.03`。
- 接缝高度：`0.025~0.035`。
- 接缝中心：两个格心的中点。
- 同类地形：连接片使用同一顶层颜色。
- 不同地形：在中线两侧各生成半块连接片，分别使用两侧颜色。
- 四格交点：额外生成小型顶面角片，避免十字交点露出背景。

连接片只填补顶面空隙，不能向下延伸到中层或底层。

### 3.3 唯一接缝

每对相邻地块只生成一次连接片。使用排序后的两个逻辑坐标生成 `top_seam_key`，避免两边重复生成导致重叠闪烁。

地块新增、转换或删除后，需要重建自身和四邻的顶层连接片。

## 4. 边框连接方案

边框负责遮住顶层外侧，而不是把整层底座围成墙。

- 相同地形相邻时，两者之间不画边框。
- 不同地形相邻时，在地形分界中线保留一条细边框。
- 面向空白、山体或缺口的一侧保留完整外边框。
- 边框长度使用 `TOP_SIZE + 0.04`，让直线端点稍微重叠。
- 四个角增加约 `0.04 x 0.04` 的角柱，消除边框转角裂缝。
- 边框宽度保持约 `0.025~0.035`，高度约 `0.10~0.14`，避免形成厚墙。

## 5. 道路棕色基层断开的原因

当前单格道路由两层组成：

- 棕色基层：宽 `0.24`，单臂长度 `0.54`，高度约 `0.165`。
- 浅黄色路面：宽 `0.20`，单臂长度 `0.54`，高度约 `0.178`。

相邻格心距离为 `1.25`。两侧道路单臂各延伸 `0.54`，所以接缝缺口为：

```text
1.25 - 0.54 - 0.54 = 0.17
```

当前 `_update_road_bridges()` 只生成一块浅黄色桥面：

- 没有生成棕色基层。
- 桥面宽度仍是旧值 `0.16`，比地块内路面 `0.20` 更窄。
- 桥面使用独立浅色材质，并带青色发光，与普通道路颜色不一致。

因此即使浅黄色中心勉强跨过接缝，棕色道路两侧仍然会明显断开，浅色路面也会在连接处突然收窄。

## 6. 道路接缝修复方案

### 6.1 每条接缝生成双层道路桥

对每一对 `_roads_connect()` 为真的相邻地块，在两格中点生成：

1. 棕色基层桥：沿道路方向长度 `0.21~0.24`，横向宽度 `0.24`，中心高度 `0.165`。
2. 浅黄色路面桥：沿道路方向长度与基层相同，横向宽度 `0.20`，中心高度 `0.178`。

沿道路方向的长度比理论缺口 `0.17` 多约 `0.04~0.07`，使桥接模型与两侧道路发生小幅重叠，避免浮点精度或斜视角产生细线。

### 6.2 统一材质

- 基层桥使用与 `_spawn_road()` 完全相同的 `#5a4430` 棕色材质。
- 路面桥使用与 `_spawn_road()` 完全相同的 `#d8bd80` 路面材质。
- 普通接缝取消青色高发光。
- 已连通道路可以整体使用非常弱的暖色 emission，但地块内路段和接缝必须一致。
- 道路闭合效果使用独立的贴地光晕，不通过改变接缝颜色表达。

### 6.3 尺寸必须按方向交换

水平道路：

```text
基层桥 size = Vector3(bridge_length, 0.026, 0.24)
路面桥 size = Vector3(bridge_length, 0.022, 0.20)
```

垂直道路：

```text
基层桥 size = Vector3(0.24, 0.026, bridge_length)
路面桥 size = Vector3(0.20, 0.022, bridge_length)
```

两层都放在两格世界坐标的中点。

### 6.4 更新与删除

- 两层道路桥共享同一个 `road_key`。
- 增加 `road_bridge_layer = "under" | "surface"` 元数据。
- 刷新道路时先删除该 `road_key` 的全部旧层，再重新生成。
- 建筑地块、山体、缺口和不存在的地块不生成道路桥。
- 道路旋转、开发地块转换和动态扩边后都要刷新相邻道路桥。

## 7. 本轮代码修改范围

确认实施时，只修改以下内容：

1. `_spawn_island_base()`：恢复中层与底层的独立尺寸。
2. `_update_edge_bridges()` / `_spawn_bridge()`：只生成顶层连接片和必要角片。
3. `_spawn_edge_trim()`：缩回细边框，并补齐转角。
4. `_update_road_bridges()`：改为棕色基层桥加浅黄色路面桥。
5. 道路材质创建：确保格内路段与接缝共享相同颜色和表现。

UI、天气、大楼和排行榜不属于这份修订文档的代码范围。

## 8. 森林中心深色圆圈遮挡道路

### 8.1 原因

森林中心的深色圆圈是 `_tile_forest_surface()` 创建的圆柱形苔藓盘。它与道路的实际高度为：

```text
苔藓盘中心 y = 0.190
苔藓盘高度 = 0.025
苔藓盘顶面 y = 0.2025

道路中心 y = 0.178
道路高度 = 0.022
道路顶面 y = 0.189
```

苔藓盘顶面比道路高约 `0.0135`，因此深度测试会让它遮住道路。渲染优先级不能可靠解决两个实体模型的高低关系，还可能造成模型隔空穿透。

### 8.2 最终方案

- 无道路的森林地块继续显示中心苔藓盘。
- 有道路的森林地块不生成完整中心苔藓盘。
- 将苔藓替换为 `2~4` 个半径约 `0.08~0.14` 的不规则小斑块。
- 每个斑块通过 `_feature_position(road_mask)` 选位，使用与树木、花朵相同的道路安全范围。
- 小斑块顶面不得高于道路基层，建议控制在 `y <= 0.170`。
- 树桩、树木和落叶起点继续避开道路。
- 道路保持贴近地面，不通过整体抬高来压住苔藓。

### 8.3 非建筑地块的显示顺序

1. 顶层地形表面。
2. 避开道路的草斑、苔藓、沙丘等贴地装饰。
3. 棕色道路基层。
4. 浅黄色道路路面。
5. 位于道路安全范围外的花朵、树木、灌木和石块。

建筑是唯一例外：建筑地块完全不渲染道路。

## 9. 验收标准

- [ ] 俯视时，相邻有效地块的顶层没有背景色缝隙。
- [ ] 四格交点没有针孔状空隙。
- [ ] 同类地形之间没有双边框。
- [ ] 不同地形仍有清楚但较细的边界。
- [ ] 中层、底层和岩芯保持单格浮岛轮廓，没有横向铺满。
- [ ] 连续道路的棕色基层在接缝处完整连接。
- [ ] 浅黄色路面在接缝处不收窄、不变色。
- [ ] 普通道路接缝没有突兀的青色发光。
- [ ] 建筑、山体和缺口边缘不出现道路残片。
- [ ] 森林道路不再被中心深色苔藓圆盘遮挡。
- [ ] 有道路的森林仍保留道路之外的小块苔藓细节。
