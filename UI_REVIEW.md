# 游戏UI评价与优化方案

---

## 当前布局分析

```
┌─────────────────────────────────────────────────────────┐
│ [顶部信息栏] 生长率/升级降级/天气 (y=8, h=36)            │
├──────────────┬──────────────────────────┬───────────────┤
│              │                          │ [右侧面板]     │
│  [卡堆×3]    │                          │ 玩家信息       │
│  y=28        │       棋盘区域           │ 守育进度       │
│              │                          │ 花朵总数       │
│              │                          │ 天气信息       │
│              │                          │ 最近操作       │
│              │                          │ 操作提示       │
├──────────────┤                          ├───────────────┤
│              │                          │               │
│  [手牌区]    │                          │               │
│  底部扇形    │                          │               │
└──────────────┴──────────────────────────┴───────────────┘
```

---

## 问题诊断

### 1. 右侧面板太满、信息堆叠
- 面板从 y=50 到 vp.y-55，高度约 600px，内容密度极高
- "守育进度"卡片 225px 高，包含进度条+种子+花朵分数条，太臃肿
- 天气信息重复（顶部信息栏已有，右侧面板又写一遍）
- 操作历史列表无底限，可能撑满面板

### 2. 卡堆位置尴尬
- 三个卡堆在左上角 (x=26, y=28)，远离手牌区
- 卡堆尺寸 94×132，占用空间但交互频率低（每回合只抽3次）
- 与手牌区之间有大片空白

### 3. 视觉层次不清晰
- 玻璃卡片半透明背景与3D场景混在一起，边界不清晰
- 字号混用（12/13/15/18/22），没有统一的层级关系
- 进度条、分数条、天气条视觉权重相似，区分度低

### 4. 信息架构混乱
- 左侧卡堆、底部手牌、右侧面板、顶部信息栏——四个方向的UI包围棋盘
- 新玩家不知道该看哪里
- 抽牌→出牌→结算的流程没有清晰的视觉引导

### 5. 操作提示被挤到角落
- "滚轮缩放·中键平移·R重开"在面板最底部，字体小，容易被忽略

---

## 优化方案

### 方案A：精简右侧面板

**目标**：右侧只保留核心信息，减少视觉噪音

```
[右侧面板 - 精简后]
┌─────────────────┐
│ ● 玩家名  出牌   │  ← 头部（保留）
│ 守育 3/30  ████  │  ← 进度条（保留，一行紧凑）
│ 种子：5          │  ← 种子数（保留）
│ P1 ████ 45      │  ← 分数条（保留）
│ P2 ████ 32      │
│ 天气：平稳       │  ← 天气（保留，删除重复的"最近操作"）
│ R重开 · Q/E旋转  │  ← 操作提示（移到面板底部）
└─────────────────┘
```

**删除**：
- 天气与顶部信息栏重复，右侧只保留一行天气状态
- "最近操作"历史列表——太长且信息密度低
- 进度条的"守育进度"标签，直接用数字

**保留**：
- 玩家头像+阶段
- 进度条（一行）
- 种子数
- 分数条
- 天气状态

### 方案B：卡堆移到右侧面板内

**目标**：减少UI方向，卡堆与手牌同侧

```
[右侧面板]
┌─────────────────┐
│ 玩家信息         │
│ 进度/种子/分数   │
│ 天气             │
│ ──────────────  │
│ [开发] [道路] [天气] │  ← 三个小卡堆横排
│ ──────────────  │
│ R重开 · Q/E旋转  │
└─────────────────┘
```

卡堆缩小到 60×84，三个横排排列，放在面板内进度区下方。

### 方案C：统一视觉层级

**字号体系**（当前 → 建议）：
- 面板标题：22 → **18**（缩小，不抢焦点）
- 区域标签：13 → **12**
- 正文内容：13-15 → **13**
- 辅助信息：12 → **11**
- 操作提示：12 → **10**

**颜色体系**：
- 面板背景：统一 `Color(0.08, 0.10, 0.12, 0.85)`（深色半透明，与3D场景对比明确）
- 文字主色：`Color("#e8ece8")`（浅灰绿）
- 文字辅色：`Color("#8a9a90")`（暗灰绿）
- 强调色：玩家颜色

### 方案D：手牌区优化

**当前问题**：手牌扇形展开角度太大，卡牌重叠严重

**建议**：
- 卡牌尺寸从 104×150 缩小到 **90×130**
- 扇形角度从 ±15° 缩小到 **±8°**
- 最小间距从 4px 增加到 **16px**
- 选中卡上浮幅度从 28px 增加到 **36px**

---

## 实施建议

| 优先级 | 改动 | 效果 |
|--------|------|------|
| P0 | 精简右侧面板（删重复天气、删操作历史） | 减少50%信息量 |
| P0 | 卡堆移到面板内 | 减少UI方向，布局更紧凑 |
| P1 | 统一字号/颜色体系 | 视觉层次清晰 |
| P1 | 手牌区缩小+间距加大 | 卡牌更易辨认 |
| P2 | 操作提示移到面板底部 | 不再被忽略 |
