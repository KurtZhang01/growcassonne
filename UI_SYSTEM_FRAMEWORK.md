# Terrain Minimal UI System

## 目标

UI 应像地块系统的延伸，而不是覆盖在棋盘上的另一套网页组件。整体采用浅色矿物面板、细暗边、低对比纹理和明确的卡种颜色；主要操作集中在底部，信息集中在右侧，棋盘保持最大视觉面积。

## 布局框架

- 顶部状态带：回合、阶段、天气和生长倍率，仅显示全局状态。
- 左侧预览栏：位于底部卡牌区上方，显示当前开发形状、旋转、道路和可放置状态。
- 右侧信息栏：位于底部卡牌区上方，固定显示当前玩家、守育进度、动态排名和最近四条操作历史。
- 底部卡牌轨道：横跨整个窗口宽度；左侧公共卡堆、中部手牌扇面、右侧回合操作，抽牌和出牌都发生在这里。
- 棋盘场景：始终铺满整个窗口，所有UI都是叠加在场景之上的独立半透明层，不能裁切或挤压棋盘视口。
- 顶部栏与底部栏横向占满扣除外边距后的可用宽度；左、右栏严格夹在两条横栏之间。
- 四个栏位统一保留18px屏幕外边距与10px区块间距，不接触窗口边缘。
- 栏位统一使用约40%-44%不透明度、18-19px背景模糊和浅色细描边；卡牌资产本身保持实体纸牌质感。

## Godot 磨砂实现

Godot 4 可以实现真正的高斯磨砂。界面根节点下放置一个覆盖视口的 `BackBufferCopy`，各浮层使用共享的 `ShaderMaterial`，通过 `hint_screen_texture` 读取其后方棋盘画面并做多点模糊采样。Shader 输出时再混合约 40%-44% 的浅色蒙版，边线单独保持约 38%-40%，这样棋盘仍然可见且文字对比度稳定。

- `BackBufferCopy.copy_mode` 使用 `COPY_MODE_VIEWPORT`，保证上下左右所有浮层都能采样完整棋盘。
- 模糊半径按 UI 缩放换算，桌面以 18px 左右为视觉目标；低性能设备可减少采样点而不改变布局。
- 卡牌、图标和文字不进入模糊材质，只给面板背景使用，避免内容一起变糊。
- `gl_compatibility` 渲染器支持屏幕纹理采样；正式接入时对 Windows 和 Web 导出分别检查性能。

## 上下文预览栏

- 点击公共牌库：显示该牌库各卡种和等级的抽取概率。
- 点击开发卡：显示真实开发形状、旋转角度、生成地块和道路。
- 点击建筑开发卡：显示对应建筑、占地和增益范围。
- 点击天气卡：显示天气视觉、持续时间、全局效果和互斥天气。
- 点击棋盘地块：显示地形、容量、花朵、生长率、扩散率、道路与相邻增益。
- 左侧栏只展示当前上下文，不同时堆放多组信息。

## 统一卡牌组件

公共卡堆与手牌必须共用同一个视觉组件：

1. 黑色软投影。
2. 右下露出的白色纸质衬底。
3. 无边框类别渐变。
4. 同一套斜向暗纹。
5. 同一位置的名称、主体模型和效果文字。

卡堆只通过“背后两层叠片”和较小比例表达厚度，不再使用另一套边框卡背。卡堆名称放在操作轨道标签中，最上方卡面仍使用对应类别的标准卡牌组件。

## 视觉令牌

| 用途 | 颜色 |
| --- | --- |
| 面板纸色 | `#F4F4EE` |
| 主文字 | `#17231F` |
| 次文字 | `#66736E` |
| 深边/阴影 | `#182823` |
| 播种 | `#F4F1E8` / `#4F9C62` |
| 开发 | `#A9AFB2` / `#C47A42` |
| 道路 | `#E8C34F` / `#AE8B55` |
| 天气 | `#65A9D8` / `#568EB0` |

圆角只用于卡牌和小型状态控件，半径为 6px；主信息栏保持直边。标题18px、正文13px、辅助11px，文本必须测量后截断。

## 交互状态

- 卡堆可抽取：整体上移4px，投影加深；不可抽取：饱和度降低。
- 手牌悬停：上浮18px并减少遮罩；选中：上浮34px、放大1.06并置顶。
- 当前玩家：只用玩家色竖线和圆点标识，不给整个面板染色。
- 重要播报：在右侧栏底部单行更新，不常驻多行日志。

## 接入状态

- `scripts/main3d.gd` 已将 `_draw_public_decks()` 与 `_draw_hand_card_face()` 收敛到共享的 `_draw_card_component()`。
- 顶栏、左侧预览、右侧信息和底部卡牌轨道已按本规范叠加在全屏棋盘上。
- 左侧预览已接通牌库概率、开发与建筑模型、天气效果以及实时地块属性。
- `shaders/ui_frosted_glass.gdshader` 负责屏幕纹理的多点模糊和浅色磨砂混合。
- `ui-system-preview.html` 保留为布局和交互参考，Godot 内的卡牌及地块图案继续使用游戏脚本中的现有资产绘制方式。
