# GrowCassonne 地块、建筑与 UI 优化方案

## 本次确认后的实施补充

- UI 和手牌采用与浮岛一致的低多边形视觉语言：实色浅灰绿面板、深色侧边、薄顶面高光。保留四类卡的白、灰、黄、蓝识别色。
- 地块连接方案已按用户反馈收窄：只连接顶层与边框，中层、底层和岩芯保持独立浮岛轮廓。具体尺寸和道路双层接缝见 `TILE_TOP_AND_ROAD_SEAM_PLAN.md`。
- 建筑光幕边长和中心偏移分别使用 TILE_SPACING 和 TILE_SPACING/2，以保证转角相接。
- 操作播报记录玩家、动作和结果，保留最近四条；使用实测字体宽度截断，不能越过面板。
- 排行榜按花朵数稳定排序，数值和行位置缓动更新，同分保持顺序。
- 彩虹使用 0 到 PI 的七色三角形带，仅显示上半圆；沙尘暴增加低空风沙带和较大颗粒，旱季增加暖色热浪带，效果随镜头覆盖可见区域。
- 本轮只能进行不启动 Godot 的检查；不得把 git diff --check 称为引擎解析或视觉验收通过。

## 目标

本轮优化解决五类问题：地块整体仍有拼缝、洪山科技大厦造型异常、道路层级不正确、卡牌与右侧 UI 排版粗糙，以及缺少能够动态反映玩家花朵排名的统计组件。

本文件经确认后进入实施。以下原始方案保留为设计目标；本次实际变更见补充记录，未勾选项仍需要运行验收。

---

## 1. 地块真正连续化

### 1.1 当前问题

目前的“连接”只处理了窄边框和少量桥接块，没有统一处理完整地块结构，因此仍能看到：

- 相邻方形顶板之间有缝。
- 顶层连接片与地形表面的尺寸不一致，四格交点可能露出背景。
- 建筑增益光幕逐格生成，边角断开且高度、亮度不足。
- `_spawn_bridge()` 使用较窄的条状模型，无法覆盖两个地块间完整接缝。

### 1.2 最终连接范围

只连接顶层地形表面和顶层边框。中层、底层和岩芯保持每格独立的收缩轮廓，装饰物仍独立放置在各自地块上。

每对上下左右相邻的有效地块只生成：

1. **表面连接板**：连接两个地块的顶面，宽度覆盖完整接缝。
2. **边框连接与角片**：封闭不同地形的分界和四格交点。

同类地形使用单一颜色，形成完整区域；不同地形使用左右或前后各占一半的过渡连接件，保留地形边界但不留下空气缝。

### 1.3 数据与函数调整

- 将 `_spawn_bridge()` 改为 `_spawn_tile_seam(owner_pos, neighbor_pos)`。
- 每条接缝只生成一次，使用排序后的坐标作为 `seam_key`，避免双重模型和闪烁。
- 接缝模型存放在 `edge_root`，并带有 `tile_seam`、`seam_key` 元数据。
- `_update_edge_bridges()` 改为先删除受影响接缝，再按照当前两侧地形完整重建。
- 地块新增、开发、天气转换和动态扩边后，都刷新自身及四邻接缝。
- 山体可以与山体连接；缺口保留半透明顶层连接；不存在地块不生成连接件。
- 具体尺寸、层级解释和道路双层接缝以 `TILE_TOP_AND_ROAD_SEAM_PLAN.md` 为准。

### 1.5 光幕连续化与增强

建筑增益范围先计算成一个 `boosted` 集合，再只绘制该集合的**外轮廓**：

- 相邻增益格之间不绘制内部边。
- 外轮廓四角增加短连接柱，消除直角处的断口。
- 光幕高度提高至 `0.32`，中心高度约 `0.38`。
- 宽度从 `0.024` 增加到 `0.045`，透明度提高到约 `0.38`。
- 发光强度在 `0.55-1.05` 之间缓慢呼吸。
- 金色粒子沿整个外轮廓移动，而不是每格随机散布。
- 同一连通增益区域共享材质和 Tween，保证脉冲同步。

### 1.6 验收标准

- 从四个斜视方向观察，相邻方框的顶面没有空气缝。
- 中层、底层与岩芯仍能看出单格浮岛轮廓。
- 同类地块看起来是连续地貌，不出现双边框。
- 不同地形边界清楚，但中间不能看到背景。
- 建筑光幕形成完整闭合外轮廓，拐角无缺口，正常缩放下清晰可见。

---

## 2. 洪山科技大厦重新建模

### 2.1 当前问题

- 某个方向出现特别长的墙，是局部坐标、朝向向量和长宽切换混用造成的。
- 白色结构件跨越整个立面，视觉上像一堵墙，而不是幕墙框架。
- 立面重复度过高，缺少入口、层次、窗格、设备层和屋顶特征。
- 两格建筑虽然相邻，但建筑语言仍像两个独立方块。

### 2.2 建模原则

新模型统一在“建筑局部坐标系”中生成：

- `forward`：两格建筑的连接方向。
- `right`：与连接方向垂直。
- 所有尺寸先按局部 `width/depth` 定义，再通过辅助函数转换为世界 X/Z。
- 禁止在不同分支里手工交换 `Vector3.x` 与 `Vector3.z`。

新增辅助函数：

```gdscript
func _oriented_size(width: float, height: float, depth: float, along_x: bool) -> Vector3
func _oriented_offset(right: float, up: float, forward: float, direction: Vector2i) -> Vector3
```

这能从结构上消除“某一面墙特别长”的问题。

### 2.3 新建筑构成

两格合成一个完整建筑群：

1. **连续基座**：白色石材裙房贯穿两格，中部不留缝。
2. **中央大厅**：靠两格接合处设置通高玻璃中庭和横向连廊。
3. **主塔与副塔**：主塔 8 层、副塔 6 层，高度和退台节奏不同。
4. **幕墙窗格**：每层分成 3-4 个玻璃窗格，使用深浅两种蓝灰玻璃交替。
5. **白色框架**：只放在角柱、楼层线和入口框架，不生成覆盖整面的白墙。
6. **入口雨棚**：面向建筑外侧，加入两根细柱和明确入口暗部。
7. **屋顶设备层**：低矮白色设备盒、玻璃冠部和小型标志结构。
8. **景观平台**：小灌木、铺装和座椅控制在地面层，不遮挡立面。

### 2.4 立面变化

- 主立面采用“玻璃-白框-玻璃”的三段式构图。
- 两侧立面增加竖向遮阳片，间距均匀但长度错落。
- 中央中庭使用更亮的青蓝玻璃，塔楼使用低饱和蓝灰玻璃。
- 部分楼层设置内退露台，打破连续方盒子的单调感。
- 白色框架宽度限制在 `0.025-0.05`，不得出现贯穿整块地面的超长墙。

### 2.5 验收标准

- 四种旋转方向下，模型比例一致，不出现异常长墙或翻转立面。
- 两格之间看起来是一座建筑群，并有明确连接结构。
- 正常游戏镜头下能辨认入口、双塔、中央中庭和幕墙分格。
- 任何结构不得越过两格建筑整体占地边界过多。

---

## 3. 道路渲染层级

### 3.1 规则

- 道路必须覆盖草地斑块、灌木、花朵、森林地表和荒漠石块等装饰。
- 建筑地块不显示道路，也不生成通向建筑的道路桥接模型。
- 道路不能通过简单提高到很高的位置解决，否则会再次悬浮。

### 3.2 实现方式

地块内部采用稳定的高度层级：

| 内容 | 建议表面高度 |
|---|---:|
| 地块顶面 | 0.160 |
| 道路基层 | 0.172 |
| 道路路面 | 0.188 |
| 花朵/灌木根部 | 0.195 |

装饰物生成后执行道路避让：

- `_feature_position()` 的道路安全半宽从约 `0.18` 调整到 `0.23`。
- 已有花朵在修路后通过 `_displace_flowers_for_road()` 平滑移位。
- 草地斑块、石块和树木创建时就避开道路，不需要整格刷新。
- 森林中心圆形苔藓盘在有道路时替换为多个避路小斑块，且斑块顶面低于道路基层。
- 道路材质设置适度的 `render_priority` 或透明深度规则，仅用于解决共面闪烁，不依赖它遮盖实体模型。

### 3.3 验收标准

- 道路表面完整可见，不被草皮、石块或树根覆盖。
- 道路仍紧贴地表，不出现明显悬浮。
- 建筑地块和建筑边缘不出现道路残片。
- 森林苔藓、草斑和沙丘不得遮住道路表面。

---

## 4. 卡牌与右侧 UI 重排

### 4.1 视觉方向

继续使用克制的半透明磨砂风格，但减少大圆角和无意义留白：

- 主面板圆角 `8px`。
- 卡牌圆角 `6px`。
- 颜色仍按播种白、开发灰、道路黄、天气蓝区分。
- 暗纹降低对比度，避免干扰标题与等级。
- 所有交互状态使用边框、阴影和轻微位移表达，不放大卡牌导致布局跳动。

### 4.2 防止文字越界

建立统一文字绘制辅助函数：

```gdscript
func _draw_fitted_text(text: String, rect: Rect2, font: Font, max_size: int, color: Color, align := HORIZONTAL_ALIGNMENT_LEFT)
```

处理规则：

- 先测量 `font.get_string_size()`。
- 从最大字号逐级缩小到最小字号，直到宽度适合容器。
- 描述文字按词或中文字符自动换行，最多两行。
- 超过两行时截断并加省略号。
- 卡牌标题、等级、说明分别使用固定区域，不能共享同一基线。
- 所有文字距离卡牌边缘至少 `10px`。
- 右侧面板根据可用高度压缩间距，而不是让文字溢出。

### 4.3 卡牌布局

- 底部手牌继续按斗地主方式重叠，右侧卡牌在左侧卡牌上方。
- 悬停只上移 `14-18px`，不改变卡牌宽高。
- 被激活卡牌从手牌中隐藏，同时在棋盘边缘显示一个小型“待放置卡”标识。
- 卡牌标题置顶，中央显示图案，底部显示等级和一句短效果。
- 长名称采用动态字号，例如“二级建筑开发”不得挤出卡面。

### 4.4 右侧面板分区

右侧 UI 从上到下固定为：

1. 当前玩家与回合。
2. 当前阶段和剩余抽牌次数。
3. 玩家花朵动态排名。
4. 当前天气与持续回合。
5. 最近一次结算信息。

每个分区使用无嵌套的独立带状区域，不再堆叠多层卡片。

### 4.5 响应式规则

- `UI_SIDEBAR_WIDTH` 改为在 `300-360px` 之间自适应。
- 低于一定屏幕宽度时，右侧栏缩窄并降低字体一级。
- 高度不足时优先缩小垂直间距，其次隐藏次要说明，不隐藏关键数值。
- 卡牌可用宽度必须扣除右侧栏实际宽度。
- 所有文本在 1280×720、1920×1080 和窄屏比例下都不得越界。

---

## 5. 玩家花朵动态排行榜

### 5.1 展示内容

右侧新增“花朵排名”区域，每名玩家对应一条横向统计条：

- 玩家颜色圆点。
- 玩家名称。
- 当前花朵总数。
- 横向进度条。
- 当前名次。
- 本回合增量，例如 `+12`，结算后短暂显示。

最高玩家位于最上方，其余依次向下。

### 5.2 排序规则

每次 `_settle_turn()` 完成并重新计算分数后更新排名：

1. 花朵总数高者在前。
2. 同分时保持上一轮相对顺序，避免统计条来回跳动。
3. 开局同分按玩家编号排列。

需要新增：

```gdscript
var ranking_order := [0, 1, 2, 3]
var previous_scores := [0, 0, 0, 0]
var ranking_offsets := {}
var ranking_target_offsets := {}
```

排序必须稳定，不能直接依赖不稳定的匿名比较结果。

### 5.3 排名移动动画

- 结算前记录每名玩家原来的行位置。
- 结算后计算新的排序位置。
- 使用 `0.45-0.60s` 的缓出动画，把每条统计条移动到新位置。
- 上升玩家先轻微提亮，下降玩家只移动，不使用负面红色闪烁。
- 数字从旧值平滑计数到新值。
- 本回合增量在条末端显示约 `1.5s` 后淡出。

因为当前 UI 使用 `_draw()` 即时绘制，动画不应直接依赖 Control 节点移动。推荐在 `_process()` 中插值每名玩家的 `ranking_offsets[player_id]`，然后 `_draw_player_ranking()` 使用该偏移绘制。

### 5.4 进度条比例

- 进度条基准为当前最高花朵数。
- 第一名填满可用宽度，其他玩家按 `score / max_score` 显示。
- 当所有玩家都是 0 时，所有条显示最小底线，不除以 0。
- 最小可见长度约为轨道宽度的 `4%`，保证玩家颜色仍可识别。

### 5.5 接入点

- `_start_game()`：初始化顺序、旧分数和动画位置。
- `_settle_turn()`：结算前保存旧分数，结算后更新排序目标。
- `_process()`：插值统计条位置和显示数值。
- `_draw_ui()`：调用 `_draw_player_ranking()`。
- 游戏结束界面复用最终 `ranking_order`。

### 5.6 验收标准

- 每次回合结算后，排名按照花朵总数正确排序。
- 玩家条平滑交换位置，不瞬移、不重叠。
- 同分时顺序稳定。
- 任何玩家名称和数值都不会超出右侧栏。
- 2、3、4 人模式下区域高度和间距均正常。

---

## 6. 实施顺序

### 第一阶段：结构正确性

1. 只重写地块顶层和边框接缝，恢复中层、底层的独立轮廓。
2. 修复道路与装饰层级。
3. 用统一局部坐标重建洪山科技大厦。

### 第二阶段：视觉反馈

1. 重做建筑增益光幕外轮廓。
2. 调整光幕粒子路径和亮度。
3. 检查四种建筑旋转方向。

### 第三阶段：UI

1. 建立文本测量、缩放和截断工具。
2. 重排卡牌内容与右侧信息带。
3. 增加动态花朵排行榜。
4. 检查多分辨率布局。

---

## 7. 最终检查清单

- [ ] 同类地块只连接顶板；中层和底层保持独立浮岛轮廓。
- [ ] 不同地形之间没有可看到背景的缝隙。
- [ ] 装饰地表板覆盖接缝，但不跨入其他地形。
- [ ] 建筑光幕闭合、同步、清晰且没有内部重复线。
- [ ] 洪山科技大厦四个方向均无异常长墙。
- [ ] 建筑具有双塔、中庭、入口和幕墙细节。
- [ ] 道路位于非建筑地块装饰物之上且不悬浮。
- [ ] 卡牌和右侧 UI 的所有文字均在框内。
- [ ] 排名条在结算后按花朵数平滑重排。
- [ ] 2-4 人及常见分辨率下布局不重叠。
- [ ] 在用户允许的环境中完成 Godot 实际运行与画面验收。
